<Window x:Class="MtgBinder.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MtgBinder.Wpf"
        xmlns:views="clr-namespace:MtgBinder.Wpf.Views"
        xmlns:logging="clr-namespace:MtgBinder.Wpf.Logging"
        mc:Ignorable="d"

        Title="MainWindow" Height="450" Width="800" WindowState="Maximized">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TabControl Grid.Row="1">
            <TabItem Header="Cards">
                <views:MainCardSearchView DataContext="{Binding MainCardSearchViewModel}" />
            </TabItem>
            <TabItem Header="System / About">
                <views:SystemInfoView DataContext="{Binding SystemPageViewModel}" />
            </TabItem>
            <TabItem Header="Logs">
                <logging:UiLoggingControl></logging:UiLoggingControl>
            </TabItem>
        </TabControl>

        <StatusBar Grid.Row="2" HorizontalAlignment="Left">
            <StatusBarItem Content="{Binding LatestLogMessage}" />
        </StatusBar>
    </Grid>
</Window>