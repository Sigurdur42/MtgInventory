<UserControl x:Class="MtgInventoryWpf.CardListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:MtgInventoryWpf"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <Style x:Key="groupheaderstyle" TargetType="{x:Type GroupItem}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type GroupItem}">
                        <Expander IsExpanded="True">
                            <Expander.Header>
                                <TextBlock Text="{Binding Name}" />
                            </Expander.Header>
                            <ItemsPresenter />
                        </Expander>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Label VerticalAlignment="Center">Card Name:</Label>
            <TextBox Grid.Column="1" Text="{Binding SearchToken}" VerticalAlignment="Center"/>

            <StackPanel Orientation="Horizontal" Grid.Column="2" >
                <Button  VerticalAlignment="Center" >Search!</Button>
                <Button Margin="20,0,0,0" VerticalAlignment="Center"
                        Click="OnPasteFromClipboard"
                        ToolTip="{Binding PasteButtonToolTip, Mode=OneWay}">Paste!</Button>
            </StackPanel>



        </Grid>

        <StackPanel Orientation="Horizontal" Grid.Row="1">
            <TextBlock Text="{Binding CurrentDeckSummary, Mode=OneWay}" />
        </StackPanel>

        <DataGrid ItemsSource="{Binding CardLineItems}" Grid.Row="2" Grid.ColumnSpan="3" AutoGenerateColumns="False" >
            <DataGrid.GroupStyle>
                <GroupStyle ContainerStyle="{StaticResource groupheaderstyle}">
                    <GroupStyle.Panel>
                        <ItemsPanelTemplate>
                            <DataGridRowsPresenter />
                        </ItemsPanelTemplate>
                    </GroupStyle.Panel>
                </GroupStyle>
            </DataGrid.GroupStyle>
            <DataGrid.Columns>

                <DataGridTextColumn Header="Qty" Binding="{Binding Quantity}" />
                <DataGridTextColumn Header="Name" Binding="{Binding Card.Name}" />
                <DataGridTextColumn Header="Type" Binding="{Binding Card.TypeLine}" />

                <DataGridTemplateColumn Header="Set">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <!--<TextBox Text="{Binding Card.SetCode}" Width="40" ToolTip="{Binding Card.SetName}"/>-->
                                <ComboBox Width="80" DropDownOpened="ComboBox_DropDownOpened"
                                          ItemsSource="{Binding Reprints}"
                                          DisplayMemberPath="SetCode" SelectedItem="{Binding Card, Mode=OneWay}"
                                          SelectionChanged="OnChangeSetOfCard"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Oracle" Binding="{Binding Card.OracleText}" />

            </DataGrid.Columns>
        </DataGrid>

        <!--<DataGrid Grid.Row="1" Grid.ColumnSpan="3" AutoGenerateColumns="False"
                  ItemsSource="{Binding DeckReaderResult}"
                  VerticalScrollBarVisibility="Visible"
                  HorizontalScrollBarVisibility="Visible">
            <DataGrid.Columns>

                <DataGridTextColumn Header="Name" Binding="{Binding Name}" />

                <DataGridTemplateColumn Header="Birthday">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <DatePicker SelectedDate="{Binding Birthday}" BorderThickness="0" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

            </DataGrid.Columns>
        </DataGrid>-->
    </Grid>
</UserControl>
